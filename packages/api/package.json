{
  "name": "@mycel/api",
  "version": "0.1.0",
  "private": true,
  "description": "HTTP API layer for Mycel – Cloud Run entrypoint",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist *.tsbuildinfo",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "@hono/node-server": "^1.14.0",
    "@hono/zod-openapi": "^0.19.10",
    "@mycel/core": "*",
    "@mycel/schemas": "*",
    "@mycel/shared": "*",
    "hono": "^4.7.0",
    "jose": "^5.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
