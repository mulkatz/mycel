{
  "name": "@mycel/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types, utilities, and logger for Mycel",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "pino": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "typescript": "^5.4.0"
  }
}
